#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

int main()
{
    int i=1;char s[30],a[20];
while (getch()!='\n')
{
    gets(s);
    gets(a);
    puts(s);
    puts(a);
}

    
    return 0;
}