#include <stdio.h>
#include <conio.h>

int main()
{
    int list[100],num,x,n;
    printf("Enter the number as you wish : ");
    scanf("%d",&num);
    x=2;
    if(num/x==(float)num/x)
    {
        num/=x;
        list[0]=x;
        while (num%n==0 && n>x)
        {
            x++;
        }
        
    }

    return 0;
}